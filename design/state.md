# State — Agent self-refinement

> Lean snapshot. Overwritten at each checkpoint. Read FIRST when resuming agent-self work.
> This is the workshop for improving the agent itself — separate from designing real ideas
> (those live in `projects/`).

**Status:** Fresh install. The agent's core design is inherited and locked (see
`design/decisions/` — ADRs 0001–0018). Nothing in-progress yet.

## Done
- Inherited a complete, stress-tested operating contract: kernel, methodology, container model,
  and the three operating verbs (orient / checkpoint / diagnose).

## Next (immediate)
- Nothing pending. Either start designing a real idea (just describe it — the agent will spin up
  a `projects/<name>/` container), or run **diagnose** if you want to tune the agent first.

## Open threads / blockers
- None. See `design/open-questions.md` if/when questions accumulate.

## Where we left off
Clean starting point. Use the agent; come back to `design/` only when you want to change how the
agent itself works.

## Active artifacts (open these next)
- `design/decisions/` — the ADRs (the "why" behind the agent's shape).
