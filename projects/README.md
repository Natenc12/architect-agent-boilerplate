# Projects

Reserved. Each real idea the user wants to design gets its own isolated container here
(`projects/<name>/`) — holding all and only that idea's context.

**None yet.** The current focus is building the agent itself, which happens in `design/` (one
level up), not as a project. The first project container gets created the first time the user
brings a real idea. Its internal structure is already defined — see
`methodology/project-container.md` (and `design/decisions/0004`).
